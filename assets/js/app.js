$( document ).ready(function() {
    $('.carousel').carousel({
	  interval: 2000
	});
	// $( ".nav-pills li" ).on('click',function() {
	//   $('.tabs .tab-content .tab-pane.active').hide();
	//   $('.tabs .tab-content .tab-pane').removeClass('active');
	//   var id = $(this).find('a').first().attr('href');	  
	//   $(id).show('slide', {direction: 'left'}, 1500);
	//   $(id).addClass('active');
	// });
});